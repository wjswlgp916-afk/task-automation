# HWP(한글) 파일 편집 시 반드시 지킬 것

이 저장소(`quote_automation`)는 원본 `.hwp` 양식을 바이트 단위로 편집해서
새 문서를 만든다(리눅스 환경이라 한글 프로그램으로 직접 열어볼 수 없음).
아래는 실제 사용자가 한글에서 여러 번 테스트하며 확인한, **비용이 많이 든
교훈**이다. 새 HWP 문서/편집 로직을 만들기 전에 반드시 먼저 읽을 것 —
안 그러면 같은 시행착오를 반복하게 된다.

## 0. 제일 중요한 규칙 (문서 보안설정 [높음]에서 파일이 안 열리는 문제)

**증상**: 생성한 .hwp 가 한글 "문서 보안 설정"이 [보통]/[낮음]일 때는 잘
열리는데 [높음]일 때만 "파일이 손상되었습니다" 하며 안 열림.

**진짜 원인 (실측으로 확정)**: 문단의 글자 수(nchars)를 바꾸면서
`PARA_LINE_SEG`(줄 나눔 캐시, tag=69) 레코드를 그대로 두는 것.
단, **그 문단이 원래 2줄 이상(LINE_SEG 세그먼트 2개 이상)으로 나뉘어
있던 경우에만** 문제가 된다.

- 원래 **1줄**뿐이던 짧은 문단(대학명·발급일자 같은 필드)은 글자 수가
  달라져도 낡은 LINE_SEG 를 그대로 둬도 **문제없다**.
- 원래 **2줄 이상**이던 문단(긴 본문 등)은 글자 수가 바뀌면 낡은
  LINE_SEG 를 반드시 정리해야 한다. **전체를 통째로 다시 쓰든
  (`set_plain_text`), 부분만 잘라 편집하든 (`replace_literal_everywhere`)
  상관없이 똑같이 문제가 된다** — "어떻게 편집하는가"가 아니라
  "글자 수가 바뀐 채로 낡은 다중 세그먼트 LINE_SEG 를 방치하는가"가
  핵심이다.
- **메모(CTRL_HEADER) 삭제 자체도 문단 글자 수를 바꾼다**(인라인 필드
  마커 8-WCHAR 블록이 사라지므로). 메모를 지운 뒤 그 문단을 더 이상
  건드리지 않는다면(예: K+U 결합처럼 본문 문구를 안 바꾸는 경우), 그
  문단도 낡은 LINE_SEG 정리 대상이라는 것을 잊기 쉽다 — 실제로 이걸
  놓쳐서 "K단독/U단독은 열리는데 결합만 안 열리는" 버그가 났었다.

**해결 방법**: `hwp_writer.clear_stale_line_seg(records, hdr_idx)` 를
문단 텍스트를 바꾼 직후 항상 호출한다. 이 함수는 LINE_SEG 세그먼트가
2개 이상일 때만 레코드를 지우고 PARA_HEADER 의 line_seg_count 필드
(payload offset 16:18) 를 0 으로 만든다 — 원래 1개였으면 손대지 않는다
(그대로 둬도 안전하므로). 개수를 0 으로 만들면 한글이 "캐시 없음"으로
보고 줄바꿈을 새로 계산한다.

**절대 하면 안 되는 것**: 세그먼트 개수를 억지로 1개라고 거짓 선언(예:
"이 긴 문단은 1줄이다"라고 우기는 것)하면, 한글이 그 선언을 그대로 믿어
모든 글자를 한 줄에 욱여넣어 겹쳐 그린다(실제로 겪은 별개의 손상 —
"개수 0(캐시 없음)"과는 전혀 다르다). 반드시 **지우고 0**, 절대
"거짓으로 1"이 아니다.

### 메모(CTRL_HEADER)는 사실 무관하다 — 헛다리였음

한때 "메모 앵커(CTRL_HEADER, id=`knu%`)를 지우면 무조건 보안설정
[높음]에서 막힌다"는 가설을 세웠었다. 실제 한글에서 메모를 직접
삭제(우클릭 → 메모 삭제)해 저장한 파일과 원본을 바이트 단위로 비교해본
결과, 한글 자신도 메모를 지울 때 CTRL_HEADER/MEMO_LIST 를 통째로
제거하고 감싸여 있던 텍스트는 그대로 유지하며, LINE_SEG 는 전혀
재계산하지 않고 그대로 둔다 — 즉 `hwp_writer.strip_memo_controls()`
가 이미 하는 일과 구조적으로 거의 동일했다. **메모를 지우는 것 자체는
전혀 문제가 아니다.** 다만 메모 제거로 인해 그 문단의 글자 수가 바뀌므로,
위 0번 규칙(낡은 LINE_SEG 정리)은 여전히 적용된다.

## 1. 문제를 좁혀나간 순서 (다음에 비슷한 버그가 나면 이 순서로)

원인 불명의 "보안설정 [높음]에서만 안 열림" 버그가 다시 나오면, 아래
순서로 실제 파일을 만들어 사용자에게 [높음]에서 열어봐 달라고 요청해
좁혀나간다(이번에 실제로 효과가 있었던 순서):

1. **원본 템플릿을 전혀 안 건드리고 그대로 재포장**(cfbf 로 읽고 다시
   쓰기만) → 열리면 컨테이너 재작성 자체는 안전한 것.
2. **아주 간단한 텍스트 치환 하나만** (예: 대학명 한 군데) → 열리면
   `replace_literal_everywhere` 류의 단순 치환은 안전한 것.
3. 의심되는 편집(메모 제거, 문단 다시쓰기 등)을 **하나씩만** 적용해서
   각각 따로 테스트 — 두 개 이상을 한 번에 바꾸면 어느 게 원인인지
   못 좁힌다.
4. 같은 편집이라도 **어떤 문단에 적용하는지**에 따라 결과가 다를 수
   있다(1줄짜리 짧은 문단 vs 여러 줄짜리 긴 문단) — 반드시 같은 문단,
   같은 편집으로 재현되는지 확인한다.
5. 가능하면 **한글 자신이 저장한 결과물**을 하나 받아서(사용자에게
   "한글에서 직접 편집 후 저장해서 보내달라" 요청) 우리 결과물과
   바이트 단위로 비교한다 — 어떤 필드가 다른지 보면 가설을 빠르게
   좁힐 수 있다(레코드 태그/길이 나열, 그다음 의심 레코드 hex 비교).
6. 이 환경엔 실제 한글이 없으므로, **pyhwp(hwp5)** 를 참조 파서로 쓴다:
   ```
   pip download --no-deps --no-binary :all: pyhwp   # 컴파일된 wheel 은 빌드 실패함
   PYTHONPATH=<압축푼경로>/src python3 -c "
   from hwp5.xmlmodel import Hwp5File
   list(Hwp5File('파일.hwp').xmlevents())"
   ```
   전체 파싱이 예외 없이 끝나면 최소한의 구조적 안전성은 확인된 것
   (단, pyhwp 가 한글보다 **관대한 경우**도 있고 — 이번 LINE_SEG
   버그는 못 잡음 — **더 엄격한 경우**도 있다 — 완전히 빈 인라인
   필드 wrap 은 pyhwp 도 크래시함). **pyhwp 통과 ≠ 한글에서 확실히
   열림.** 최종 확인은 항상 실제 한글([높음] 보안설정 포함)로 한다.

## 2. HWP 5.0 형식 요약 (레코드 구조)

- 파일은 CFBF(OLE 복합 문서) 컨테이너 (`cfbf.py` 로 읽고 씀).
- `BodyText/Section0` 스트림이 zlib(raw deflate, `-15`)로 압축된 본문
  레코드 스트림 (`FileHeader` 바이트 36:40 의 flags 비트0 = 압축 여부).
- 레코드 헤더: `tag(10bit) | level(10bit) | size(12bit)`, size 가
  0xFFF 면 다음 4바이트가 실제 크기.
- 주요 태그: `PARA_HEADER=66, PARA_TEXT=67, PARA_CHAR_SHAPE=68,
  PARA_LINE_SEG=69, PARA_RANGE_TAG=70, CTRL_HEADER=71, LIST_HEADER=72,
  TABLE=77, MEMO_LIST=93`.
- `PARA_HEADER` payload(24바이트): `nchars(4, bit31=마지막 문단 플래그)
  control_mask(4) para_shape_id(2) style_id(1) divide(1)
  char_shape_count(2) range_tag_count(2) line_seg_count(2)
  instance_id(4)`.
- `PARA_LINE_SEG` payload: 36바이트 세그먼트 배열 — 세그먼트 하나가
  `chpos y height height_text height_baseline space_below x width
  a8 flags` (전부 int32/uint16 섞임, `hwp_writer.clear_stale_line_seg`
  참고).
- 인라인 제어문자(누름틀/필드 begin·end 등)는 8-WCHAR 블록을 차지한다
  (`_EIGHT_WIDE_CTRL` 집합, `hwp_writer.text_of()` 가 건너뛴다).
- 문단이 "마지막 문단 in list"인지는 `nchars` 의 `bit31`(0x80000000)
  로 표시 — 이 비트를 실수로 지우거나 옮기면 "파일이 손상되었습니다"
  오류가 난다(계약서 디버깅에서 실제로 겪음).

## 3. 안전하다고 이미 검증된 편집 패턴

- **셀/필드를 순수 텍스트로 교체**: `set_plain_text()` — 끝 문단부호
  유지, nchars 만 갱신. 짧은 1줄 필드에는 그대로 안전.
- **문장 안 일부만 치환**: `replace_literal()` /
  `replace_literal_everywhere()` — 단, **문단 하나당 첫 매치 1번만**
  바꾼다(같은 문단에 같은 문구가 여러 번 있으면 나머지는 못 찾음 —
  반복 호출해서 누적해야 함, `_replace_all` 류 헬퍼 참고).
- **긴(2줄 이상) 문단의 텍스트를 바꾼 뒤에는 반드시**
  `clear_stale_line_seg()` 호출 (0번 규칙).
- **표 행 삭제/재번호**: 원본 행을 그대로 골라 셀 병합·글자모양까지
  보존하고, 남은 행만 0..N-1 로 재번호. TABLE 레코드의 rowSize 배열도
  같이 갱신해야 한다 (`hwp_writer._rebuild_item_table` 참고).
- **하이라이트(형광펜) 제거**: `PARA_RANGE_TAG` 를 지울 때, 그 문단의
  PARA_HEADER 의 range_tag_count 필드도 같이 줄여야 한다(안 그러면
  손상).
- **메모(코멘트) 완전 제거**: `strip_memo_controls()` — CTRL_HEADER 와
  MEMO_LIST 를 통째로 지우고, 감싸여 있던 문단은 순수 텍스트로 정리한다.
  한글 자신의 메모 삭제 동작과 구조적으로 동일함을 실측으로 확인함.
  **메모가 5개 이상 걸린 긴 문단이라면, 제거 후 반드시
  clear_stale_line_seg() 도 호출할 것** (제거만으로 끝내면 안 됨).

## 4. 검증 습관

- 편집 후 항상 **재파싱해서 `serialize_records(records) == data`**
  같은 라운드트립 검증을 한다 (기존 `_validate` 패턴).
- 테스트에 **글자모양(CHAR_SHAPE) 위치·개수 정합**, **필드 마커
  begin/end 균형**, **표 행수/열 타일링** 등 구조 불변식 검증을
  넣어둔다(`test_documents.py` 의 `_assert_*_structure_ok` 류 참고).
- 100자 넘는 문단을 편집했다면, 그 문단의 LINE_SEG 가 "1개로
  거짓 선언"되지 않았는지(겹침 위험) **그리고** "낡은 채로 2개 이상
  남아있지 않은지"(보안설정 [높음] 위험) 둘 다 확인하는 회귀 테스트를
  추가한다.
